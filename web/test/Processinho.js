'use strict';

import Processinho from '../src/Processinho';

describe('Processinho', function () {
    it('should be tested', function () {
        // assert.fail('No tests for this module yet.');
        if (Processinho) {
            assert.isTrue(true);
        }
    });
});
