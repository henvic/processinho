{namespace Templates.Processinho}

/**
 * This renders the main element.
 * @param? elementClasses
 * @param id
 * @skipUpdates elementClasses
 */
{template .content}
	<div id="{$id}" class="processinho component{$elementClasses ? ' ' + $elementClasses : ''}">
	</div>
{/template}
